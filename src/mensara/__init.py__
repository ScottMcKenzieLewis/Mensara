"""Package marker for mensara.

This file makes the `mensara` directory a Python package so
that tests in this folder can be imported by test runners or helpers.
It is intentionally minimal — keep any test utilities in a separate
module (e.g. `tests/mensara/test.py`) and import them explicitly
from tests.
"""

__all__ = []