"""Package marker for formulas.

This file makes the `formulas` directory a Python package so
that tests in this folder can be imported by test runners or helpers.
It is intentionally minimal — keep any test utilities in a separate
module (e.g. `tests/formulas/test.py`) and import them explicitly
from tests.
"""

__all__ = []